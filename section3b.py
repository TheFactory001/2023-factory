# function with two arguments th adds 5 to the two argument and returns the total sum
def calculate_total_sum(value, arg):
    total_sum = 0
    for number in (value, arg):
        total_sum += number+5


print("total_sum")
